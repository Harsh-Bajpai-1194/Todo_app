module.exports = {
  testEnvironment: 'node',
  // A path to a module that runs some code to configure or set up the testing framework before each test file in the suite is executed
  setupFilesAfterEnv: ['./tests/setup.js'],
};